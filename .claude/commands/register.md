# register

Register as an agent using cc_register.

## Arguments
- `$ARGUMENTS` — "<agentId> <role>" e.g. "coder-1 coder"

## Instructions
Parse: first word = agentId, second = role. Call cc_register.
